---
const { offre } = Astro.props;
---

<article class="border rounded-lg p-4 shadow-sm">
  <h2 class="text-4xl font-bold">{offre.nomMaison}</h2>
  <p class="text-sm text-gray-600">{offre.adresse}</p>

  <div class="mt-3 flex gap-4 text-sm">
    <span>{offre.nbChambres} ch.</span>
    <span>{offre.nbSdb} sdb</span>
    <span>{offre.surface} m²</span>
  </div>

  <p class="text-blue-500 opacity-75">{offre.prix} €</p>
</article>